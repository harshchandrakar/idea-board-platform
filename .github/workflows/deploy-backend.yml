name: Deploy backend
# Ships ONLY the backend: build image -> push GHCR -> Helm-upgrade just the backend
# (readiness-gated, auto-rollback on failure via --atomic) -> then the AI agent
# runs a CANARY health check and rolls back with a plain-English RCA if the app is
# actually broken. (Infra must exist first — run "Infra — provision cluster".)
on:
  workflow_dispatch:
    inputs:
      cloud:
        description: "gcp (GKE) or aws (EKS)"
        type: choice
        options: [gcp, aws]
        default: gcp

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    env:
      SHA: ${{ github.sha }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}   # for the RCA summary (optional)
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: Install agent deps
        run: pip install -r ai/requirements.txt

      - name: Resolve registry (GHCR)
        run: echo "REG=ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push backend image
        run: |
          docker build -t "$REG/backend:$SHA" -t "$REG/backend:latest" app/backend
          docker push "$REG/backend:$SHA"; docker push "$REG/backend:latest"

      - uses: azure/setup-helm@v4

      # --- point kubectl/Helm at the cluster ---
      - name: GCP auth (keyless)
        if: ${{ inputs.cloud == 'gcp' }}
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
      - name: GKE kubeconfig
        if: ${{ inputs.cloud == 'gcp' }}
        uses: google-github-actions/get-gke-credentials@v2
        with: { cluster_name: idea-board, location: asia-south1-a }

      - name: AWS auth
        if: ${{ inputs.cloud == 'aws' }}
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}
      - name: EKS kubeconfig
        if: ${{ inputs.cloud == 'aws' }}
        run: aws eks update-kubeconfig --name idea-board --region "${{ secrets.AWS_REGION }}"

      # --- rollout (readiness-gated; --atomic auto-reverts if pods never get ready) ---
      - name: Helm upgrade (backend)
        run: |
          helm upgrade --install idea deploy/helm \
            --set image.backend="$REG/backend:$SHA" \
            --set image.frontend="$REG/frontend:latest" \
            --wait --atomic --timeout 5m

      # --- AI canary: functional health check beyond readiness; rollback + RCA if bad ---
      - name: AI canary check (kubectl-based; AI RCA + rollback only if broken)
        run: |
          echo "=== pods ===";      kubectl get pods -n idea -o wide || true
          echo "=== endpoints ==="; kubectl get endpoints -n idea || true

          HEALTHY=1
          # 1) readiness/availability (reliable from the runner via kubeconfig)
          kubectl rollout status deployment/backend -n idea --timeout=150s || HEALTHY=0
          # 2) FUNCTIONAL check from INSIDE the cluster — hits the DB-backed endpoint,
          #    catching problems (e.g. a bad DB connection) the readiness probe misses.
          if [ "$HEALTHY" = "1" ]; then
            kubectl -n idea exec deploy/backend -- \
              python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/ideas', timeout=5)" \
              || HEALTHY=0
          fi

          if [ "$HEALTHY" = "1" ]; then
            echo "[canary] app healthy -> keep"; exit 0
          fi
          echo "[canary] app UNHEALTHY -> logs + AI root-cause + rollback"
          kubectl logs deploy/backend -n idea --tail=80 || true
          python -m ai.agent canary --endpoint http://localhost:8000 --namespace idea || true
          exit 1
