You are a release planner. Choose WHERE to deploy, using ONLY the options below.

Goal from the developer: "{{goal}}"

Available clouds and their targets (from platform.json):
{{delivery}}

Rules:
- Pick a cloud that exists in the options.
- Pick a target that exists for that cloud. If the goal implies free / cheap / demo,
  prefer that cloud's free_tier_target (if it has one; if not, say so in reason).
- environment is "staging" or "production" (default staging unless the goal says prod).
- Invent NOTHING — choose only from the given values.

Return JSON: {"cloud": "...", "target": "...", "environment": "...", "reason": "..."}
